// Violation.cpp
#include "Violation.h"

bool Violation::wallCollision(float x, float y, float radius) {
    // Wall collision detection code
}

bool Violation::weakHit(float ballY, float playerY, float paddleHeight) {
    // Weak hit detection code
}
